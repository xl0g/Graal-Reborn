// Package main — server entry point.
// This file is intentionally empty; the server has been split into:
//   types.go    — shared data types
//   db.go       — database layer
//   npc.go      — NPC AI and dialog definitions
//   gralat.go   — gralat spawn definitions
//   hub.go      — Hub (connection manager + game loop)
//   client.go   — WebSocket client handler
//   handlers.go — HTTP REST handlers (register, login)
//   main.go     — program entry point
package main
